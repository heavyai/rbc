#
# Usage:
#   conda env create --file=.conda/environment.yml -n rbc-dev
#   python setup.py develop
#   pytest -sv .

name: rbc
channels:
  - conda-forge
  - numba
  - defaults
dependencies:
  - python>=3.7
  - setuptools
  - numba>=0.52
  - llvmlite>=0.29
  - pytest
  - tblib
  - thriftpy2
  - netifaces
