#
# Usage:
#   conda env create --file=.conda/environment.yml -n rbc
#   python setup.py develop
#   pytest -sv .

name: rbc
channels:
  - conda-forge
  - numba
  - defaults
dependencies:
  - python>=3.7
  - setuptools
  - numba>=0.54
  - llvmlite
  - clang==11.1
  - pytest
  - tblib
  - thriftpy2
  - netifaces
